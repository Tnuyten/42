# This group is read both both by the client and the server
# use it for options that affect everything
[client-server]

# This group is read by the server
[mysqld]
datadir=/var/lib/mysql
user=mysql
bind-address=php-fpm
port=3306

# Disabling symbolic-links is recommended to prevent assorted security risks
symbolic-links=0

# Explicitly enable networking
skip-networking=0
skip-bind-address

# include all files from the config directory. Nope, this contains skip-networking?
# !includedir /etc/my.cnf.d


# To see what variables a running server is using, type
# 'SELECT * FROM INFORMATION_SCHEMA.GLOBAL_VARIABLES' instead of 'mysqld --verbose --help' or 'mariadbd --verbose --help'.
