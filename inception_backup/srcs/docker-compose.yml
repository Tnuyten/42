version: '3.7'

services:
  nginx:
    container_name: nginx
    image:
      name: nginx
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    networks:
      - inception
    volumes:
      - phpfilesV:/var/www/html
    ports:
      - 443:443
    expose:
      - 9000
    restart: always

  mariadb:
    container_name: mariadb
    build:
      context: ./requirements/mariadb
      dockerfile: Dockerfile
    networks:
      - inception
    volumes:
      - mariadbV:/var/lib/mysql
    env_file: .env
    restart: always

  wordpress:
    container_name: php-fpm
    build:
      context: ./requirements/wordpress
      dockerfile: Dockerfile
    networks:
      - inception
    volumes:
      - phpfilesV:/var/www/html
    env_file:
      - .env
    restart: always
    expose:
      - 9000
    depends_on:
      - nginx

networks:
  inception:
    name: inception

volumes:
  phpfilesV:
    driver_opts:
      type: none
      o: bind
      device: /home/tnuyten/data/PHPfiles
  mariadbV:
    driver_opts:
      type: none
      o: bind
      device: /home/tnuyten/data/mariadb
